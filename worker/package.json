{
  "name": "squarely-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker + Durable Objects backend for Squarely (see BACKEND_PLAN.md).",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.71",
    "@cloudflare/workers-types": "^5.20260828.1",
    "@types/node": "^22.20.1",
    "typescript": "~5.9",
    "vitest": "~3.2.0",
    "wrangler": "^4.127.0"
  }
}
